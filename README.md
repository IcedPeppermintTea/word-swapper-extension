# word-swapper-extension

Chrome Extension that allows you to swap words in a web page

## Roadmap

- [ ] Display error message if user hits save with empty word field
- [ ] Move functions to their own separate page / ES module
- [ ] If the word values contain regex special characters (like ., \*, +, ?, (, ), etc.), the regex can break or behave unexpectedly. Either escape the special characters or prevent them in the input value
