document.addEventListener("DOMContentLoaded", function() {
    /* Main Variables */

    let word = document.querySelector("#given_word");
    let swap = document.querySelector("#swap_value");
    
    // Get word values  and store into variables

    let page_body = document.body.innerText;


    // Get the words in the url page

    // Swap

});